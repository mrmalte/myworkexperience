Title: SDP Coder/Parser

SDP describes what a media session contains — codecs, addresses and ports — and is what two endpoints exchange when setting up a call. Teleca's IMS framework needed a more capable one.

I extended the existing SDP coder/parser with the functionality the framework required.

Implemented in C, with source in SVN.
