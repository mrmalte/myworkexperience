Title: Poker Proxy

A poker client talks to one server, but the company's players needed access to tables across several poker networks at once.

I designed and implemented the proxy that sits between the client and those networks and multiplexes the communication.

Implemented in C++/Qt on Linux, with source in SVN.
