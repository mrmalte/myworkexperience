Title: vProperty Low-Level Parser

A vObject is built from vProperties — the smallest structural unit of the format, a single property with its parameters and value.

I designed and implemented the low-level parser for them, which became the foundation of the vObject formatting framework in the preceding project.

Implemented in C on the print server's embedded Linux, with source in CVS.
